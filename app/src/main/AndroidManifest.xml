<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.haroonfazal.myapps.gpsnavigation">

    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"></uses-permission>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MyNavigationActivity"
            android:label="@string/title_activity_my_navigation"
            android:theme="@style/AppTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name=".MyLocationActivity"
            android:label="@string/title_activity_my_location" />
        <activity android:name=".StreetViewActivity" />
        <activity android:name=".RouteFinderActivity" />
        <activity android:name=".MapDetailActivity" />
        <activity android:name=".AboutActivity" />
        <activity android:name=".NearbyLocationActivity" />
        <activity android:name=".OpenPlacesActivity" />
        <activity android:name=".LocationOffActivity"></activity>






    </application>

</manifest>